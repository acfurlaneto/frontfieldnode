"use client";

import MapClient from "@/components/MapClient";

/** Client-only Leaflet surface used by the full-bleed fleet map route. */
export default function MapaFrota() {
  return <MapClient fullBleed />;
}
